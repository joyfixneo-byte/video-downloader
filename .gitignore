# Скачанные видео — не храним в репозитории
backend/downloads/
# Состояние рантайма (глобальный рекорд тетриса и т.п.)
backend/data/
# Python
__pycache__/
*.pyc
.venv/
venv/
# Локальные настройки
.env

# Локальные настройки Claude (launch.json, settings.local.json) — не в репо,
# а вот слэш-команды (.claude/commands/) versioned: это общий контекст задач.
.claude/*
!.claude/commands/
