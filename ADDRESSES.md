# Адреса сайта

Один и тот же код, два инстанса. Пароль (`DOWNLOADER_PASSWORD`) одинаковый на обоих.

| Адрес | Что это | Когда нужен |
|---|---|---|
| http://91.186.220.103:8000 | инстанс на VM **apps-vpn**, egress через VPN | YouTube и прочие сайты, которые блокируют РФ-IP |
| http://91.186.220.103:8002 | инстанс на VM **apps**, прямой egress | торренты и обычные сайты — быстрее, без узкого VPS-туннеля |
| http://192.168.88.251:8000 | тот же apps-vpn, но по локалке (portproxy на SRV-HOST) | дома: отдача файла идёт по LAN, а не через Финляндию |
| smb://192.168.88.251 (шара `VideoShare`) | SMB-библиотека готовых файлов | Apple TV / VLC — смотреть без ПК и без браузера |

Внутренние (только из LAN 10.10.0.0/24 / с хоста):

- `http://10.10.0.31:8000` — apps-vpn
- `http://10.10.0.30:8000` — apps
- на VM файлы витрины: `/mnt/videoshare`

Полезные ссылки внутри сайта:

- `/api/file/<job_id>` — прямая ссылка на готовый файл (вставляется в VLC)
- `/api/library` — список файлов SMB-витрины (под паролем)

⚠️ Всё по HTTP без TLS — пароль ходит открытым текстом. Домен + TLS в бэклоге.
